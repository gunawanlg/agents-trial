{
  "name": "scorecard-segment-eval",
  "install": "sudo apt-get update -qq && sudo apt-get install -y --no-install-recommends python3-venv && python3 -m venv .venv && .venv/bin/python -m pip install --upgrade pip && .venv/bin/pip install -e \".[dev]\""
}
